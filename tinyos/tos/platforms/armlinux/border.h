#ifndef __BORDER_H__
#define __BORDER_H__

enum {
  ROUTE_IFACE_TUN = 3,
};

// Might need to do this.
// typedef uint8_t bool __attribute__((combine(rcombine)));

// bool rcombine(bool a, bool b) {
// 	return a & b;
// }

#endif
