You'll need to call make epic install.11 to run this, else it will share the
same IPv6 address as the border router.

I'm not too familar on how IPv6 addresses are supposed to be laid out on
WSNs. Is there something special that should be done with subnets and
broadcast addresses?
